/*
 Calculate the absolute difference between max and min of 3 random numbers.
Write a program that generates 3 random numbers between 1 and 10 (both inclusive). 
Calculate the greatest and the smallest numbers and print thier absolute difference.


Examples:
3, 5, 2 ->3
7, 3, 1 ->6
5, 5, 5 ->0 
1, 10, 9 ->9 
10, 1, 2 ->9


PSEUDO CODE or (approach to solve this problem)
1. generate 3 random numbers 
2. find max and min numbers among the random numbers
3. calculate and output max - min
*/

let r1 = (Math.round(Math.random() * Math.max - Math.min) + Math.min);
console.log(r1);
let r2 = 6
let r3 = 4

console.log(Math.abs())

console.log(Math.abs(Math.round(Math.random() * 10) + 1))


// THE ANSWER 

